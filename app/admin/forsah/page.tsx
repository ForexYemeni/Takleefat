import { ForsahAdminManager } from '@/components/admin/forsah-admin'

export const metadata = { title: 'فرصة — الموارد البشرية | لوحة الإدارة — تكليفات' }

export default function AdminForsahPage() {
  return <ForsahAdminManager />
}
