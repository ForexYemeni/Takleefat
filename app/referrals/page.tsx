import { MyReferrals } from '@/components/referrals/my-referrals'

/**
 * صفحة «إحالاتي» — الجولة 75 (برنامج إحالة تكليفات — إضافي بحت)
 * رابط الدعوة الشخصي + الدعوات المباشرة + الإحصاءات + مزايا الإحالة.
 * المحتوى كله من /api/referrals/me — الأهلية والحماية على الخادم.
 */
export default function ReferralsPage() {
  return <MyReferrals />
}
